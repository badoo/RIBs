configureAndroidApp(project)

android {
    compileSdkVersion 30

    defaultConfig {
        applicationId 'com.badoo.ribs.samples.gallery'
        minSdkVersion 21
        targetSdkVersion 30
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"

    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    buildFeatures {
        compose true
    }

    composeOptions {
        kotlinCompilerVersion deps.versions.kotlin
        kotlinCompilerExtensionVersion deps.versions.compose
    }
}

tasks.withType(org.jetbrains.kotlin.gradle.tasks.KotlinCompile).all {
    kotlinOptions {
        freeCompilerArgs += "-Xopt-in=com.badoo.ribs.annotation.ExperimentalApi"
        useIR = true
    }
}

dependencies {
    implementation deps.external.dagger
    implementation deps.external.mviCore
    implementation deps.external.mviCoreAndroid
    implementation deps.external.rxrelay2
    implementation deps.external.rxandroid2

    implementation deps.android.appCompat
    implementation deps.android.constraintLayout
    implementation deps.android.design

    implementation deps.compose.runtime
    implementation deps.compose.ui
    implementation deps.compose.material
    implementation deps.compose.uiTooling
    debugImplementation deps.compose.uiToolingPreview
    implementation deps.compose.foundation

    implementation project(':libraries:rib-base')
    implementation project(":libraries:rib-compose")
    implementation project(':libraries:rib-mvicore')
    implementation project(':libraries:rib-rx2')

    implementation project(':samples:routing:back-stack')
    implementation project(':samples:routing:parameterised-routing')
    implementation project(':samples:routing:simple-routing-rib')
    implementation project(':samples:routing:transition-animations')

    implementation project(':samples:communication:coordinate-multiple')
    implementation project(':samples:communication:menu-example')

    implementation project(':samples:android-integration:dialogs')
    implementation project(':samples:android-integration:launching-activities')
    implementation project(':samples:android-integration:permissions')

    implementation project(':samples:retained-instance-store')

    implementation project(':samples:resources')
}

repositories {
    mavenCentral()
}
