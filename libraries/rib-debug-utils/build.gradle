configureAndroidLibrary(project)

android {
    namespace 'com.badoo.ribs.debugutils'
}

dependencies {
    api project(":libraries:rib-base")
}

apply from: rootProject.file('gradle/gradle-mvn-push.gradle')
